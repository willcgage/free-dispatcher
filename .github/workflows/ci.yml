name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

# Cancel superseded runs on the same ref.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Lint · Types · Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        # npm install (not ci) tolerates platform-specific optional native deps
        # — e.g. @unrs/resolver-binding-* / @emnapi from eslint-config-next's
        # resolver, which differ between the macOS dev machine and Linux CI.
        # CI only lints/types/builds the Next app — it never runs Electron, so
        # skip the ~100 MB Electron binary download (installers are built
        # separately via `npm run dist`).
        env:
          ELECTRON_SKIP_BINARY_DOWNLOAD: "1"
        run: npm install --no-audit --no-fund

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Build
        run: npm run build
