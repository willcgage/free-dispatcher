{
  "name": "freedispatcher-token-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local JWT issuer for Zello Channel API (spec §7.7). Runs on the event host alongside the Next.js app.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  }
}
