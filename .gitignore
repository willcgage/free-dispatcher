# OS / editor cruft
.DS_Store
.vscode/
.idea/

# Python virtual environments
.venv/
.venv_*/
__pycache__/
*.pyc

# Local databases and logs (never commit these)
*.db
*.sqlite
*.sqlite3
*.log

# Next.js / Node (added for v2 rebuild)
/node_modules
/.next/
/out/
*.tsbuildinfo
next-env.d.ts
.vercel
.env*
!.env.example
*.pem

# Code-signing keys / certs — NEVER commit (Apple .p8 API key, .p12 cert)
*.p8
*.p12

# electron-builder installer output (build/ now holds buildResources)
/dist/

# Local PGlite data dir
/.data/

# Zello credentials (issuer + private key) — never commit
zello-credentials.json
